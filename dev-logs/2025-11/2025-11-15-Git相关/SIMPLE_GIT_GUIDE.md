# 🚀 简单 Git 使用指南

## 📌 基础概念

- **仓库 (Repository)**: 存储代码的地方
- **提交 (Commit)**: 保存代码的快照
- **分支 (Branch)**: 独立的开发线
- **标签 (Tag)**: 标记重要版本

## 🎯 日常工作流程

### 1️⃣ 查看当前状态
```bash
git status
```
显示哪些文件被修改了，哪些文件还没提交。

### 2️⃣ 添加文件到暂存区
```bash
# 添加单个文件
git add 文件名

# 添加所有文件
git add .
```

### 3️⃣ 提交代码
```bash
git commit -m "描述你做了什么"
```
例如：
```bash
git commit -m "修复登录页面的 bug"
git commit -m "添加用户管理功能"
```

### 4️⃣ 查看提交历史
```bash
git log --oneline
```
显示所有提交记录。

## 🌿 分支操作

### 创建新分支
```bash
git checkout -b feature/新功能名
```

### 切换分支
```bash
git checkout master
```

### 查看所有分支
```bash
git branch -a
```

### 删除分支
```bash
git branch -d 分支名
```

## 🔄 回滚操作

### 回滚到稳定版本
```bash
git reset --hard v1.0.0-stable
```

### 回滚到上一个提交
```bash
git reset --hard HEAD~1
```

### 回滚到特定提交
```bash
git reset --hard <提交哈希>
```

## 📝 完整工作示例

### 场景：修复一个 bug

```bash
# 1. 查看当前状态
git status

# 2. 创建新分支
git checkout -b fix/login-bug

# 3. 修改文件...（在编辑器中修改）

# 4. 查看修改
git status

# 5. 添加修改
git add .

# 6. 提交修改
git commit -m "修复登录页面的验证 bug"

# 7. 切换回主分支
git checkout master

# 8. 查看提交历史
git log --oneline
```

## 🏷️ 标签操作

### 查看所有标签
```bash
git tag -l
```

### 查看标签详情
```bash
git show v1.0.0-stable
```

## 📊 常用命令速查表

| 命令 | 说明 |
|------|------|
| `git status` | 查看当前状态 |
| `git add .` | 添加所有文件 |
| `git commit -m "msg"` | 提交代码 |
| `git log --oneline` | 查看提交历史 |
| `git branch` | 查看分支 |
| `git checkout -b name` | 创建新分支 |
| `git checkout name` | 切换分支 |
| `git reset --hard v1.0.0` | 回滚到版本 |
| `git tag -l` | 查看标签 |

## ⚠️ 重要提示

1. **提交前检查** - 使用 `git status` 确认要提交的文件
2. **清晰的提交信息** - 写清楚你做了什么
3. **定期提交** - 不要等太久才提交
4. **谨慎回滚** - 回滚前确认无误
5. **使用分支** - 为新功能创建分支

## 🆘 常见问题

### Q: 我不小心提交了错误的代码怎么办？
A: 使用 `git reset --hard v1.0.0-stable` 回滚到稳定版本

### Q: 我想看看之前的代码是什么样的？
A: 使用 `git log --oneline` 查看历史，然后 `git show <提交哈希>`

### Q: 我想在不同的分支上工作？
A: 使用 `git checkout -b feature/name` 创建新分支

### Q: 我想删除一个分支？
A: 使用 `git branch -d 分支名`

## ✅ 总结

Git 的核心就是：
1. **修改代码** → `git add .`
2. **提交代码** → `git commit -m "描述"`
3. **查看历史** → `git log --oneline`
4. **需要回滚** → `git reset --hard <版本>`

就这么简单！🎉

