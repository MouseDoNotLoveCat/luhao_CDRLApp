# Word 文档解析器修复：施工单位/监理单位错误识别问题

## 🐛 问题描述

### 现象
在导入玉岑铁路监督通知书 Word 文档时，发现部分问题的字段识别错误：

**错误识别情况**：
- 涉及工点：YCZQ-1标 的某个工点
- 受影响问题：问题 59 和 60
- 错误类型：将**问题描述**中的文字错误识别为**施工单位**字段
- 具体错误：施工单位显示为 `安全防护人员佩戴警示`（这明显是问题描述的一部分）

### 示例
**错误识别**：
```
问题 59:
  施工单位: 安全防护人员佩戴警示  ❌ (应该是施工单位名称)
  监理单位: 中铁二院  ✅
  问题描述: (缺失部分内容)

问题 60:
  施工单位: 安全防护人员佩戴警示  ❌
  监理单位: 中铁二院  ✅
  问题描述: (缺失部分内容)
```

**预期识别**：
```
问题 59:
  施工单位: 中铁五局  ✅
  监理单位: 中铁二院  ✅
  问题描述: 安全防护人员佩戴警示...  ✅
```

## 🔍 根本原因

### 问题代码位置
`backend/app/parsers/word_parser.py` 中的三个位置：

1. **第 1300-1301 行**（三级结构，问题描述提取）
2. **第 1324-1325 行**（二级结构，问题描述提取）
3. **第 1357-1358 行**（其他情况，问题描述提取）

### 错误逻辑
```python
# ❌ 错误代码
final_contractor = self._extract_contractor(para) or \
                   (self._extract_contractor(self.paragraphs[idx-1]) if idx > 0 else None) or \
                   current_contractor
final_supervisor = self._extract_supervisor(para) or \
                   (self._extract_supervisor(self.paragraphs[idx-1]) if idx > 0 else None) or \
                   current_supervisor
```

**问题分析**：
- 代码尝试从**问题描述段落本身**提取施工单位和监理单位
- 如果问题描述中包含"施工"、"监理"等关键词，就会被错误识别

### 提取函数的正则表达式

**`_extract_contractor` 函数**（第 1060 行）：
```python
match = re.search(r'^\d+[\.．]\s*([^、，]+?)施工', para)
```
- 匹配：`1.安全防护人员佩戴警示施工...`
- 提取：`安全防护人员佩戴警示` ❌

**`_extract_supervisor` 函数**（第 1068 行）：
```python
match = re.search(r'[、，]([^、，]+?)监理', para)
```
- 匹配：`...、中铁二院监理...`
- 提取：`中铁二院` ✅（这个碰巧正确）

### 为什么会出现这个问题？

1. **设计缺陷**：问题描述段落不应该尝试提取施工单位和监理单位
2. **上下文丢失**：施工单位和监理单位应该从标段/工点行提取，并保存在上下文变量中
3. **过度匹配**：正则表达式过于宽松，匹配了问题描述中的关键词

## ✅ 修复方案

### 核心思路
**问题描述段落只使用上下文中已经提取的单位信息，不尝试从问题描述本身提取。**

### 修复代码

#### 修复 1：三级结构（第 1296-1318 行）
```python
# ✅ 正确代码
# 使用上下文中的单位信息，不从问题描述段落中提取
# 避免将问题描述中的"施工"、"监理"等词误识别为单位名称
final_contractor = current_contractor
final_supervisor = current_supervisor
```

#### 修复 2：二级结构（第 1319-1343 行）
```python
# ✅ 正确代码
# 使用上下文中的单位信息，不从问题描述段落中提取
# 避免将问题描述中的"施工"、"监理"等词误识别为单位名称
final_contractor = current_contractor
final_supervisor = current_supervisor
```

#### 修复 3：其他情况（第 1355-1377 行）
```python
# ✅ 正确代码
# 使用上下文中的单位信息，不从问题描述段落中提取
# 避免将问题描述中的"施工"、"监理"等词误识别为单位名称
final_contractor = current_contractor
final_supervisor = current_supervisor
```

### 上下文变量的来源

施工单位和监理单位应该从以下位置提取：

1. **标段/工点行**（一级编号行）：
   ```
   （一）由中铁五局施工、中铁二院监理的YCZQ-1标某工点（检查时间：2026-01-13）
   ```

2. **数字编号行**（黄百格式）：
   ```
   1.中铁五局施工、中铁二院监理的YCZQ-1标某工点（检查时间：2026-01-13）
   ```

这些信息会被保存在 `current_contractor` 和 `current_supervisor` 变量中，供后续的问题描述使用。

## 📝 修改的文件

- `backend/app/parsers/word_parser.py`
  - 第 1296-1318 行：修复三级结构的问题描述提取
  - 第 1319-1343 行：修复二级结构的问题描述提取
  - 第 1355-1377 行：修复其他情况的问题描述提取

## 🎯 预期效果

修复后：
1. ✅ 施工单位字段只包含施工单位名称（如："中铁五局"）
2. ✅ 监理单位字段只包含监理单位名称（如："中铁二院"）
3. ✅ 问题描述字段包含完整的问题描述文本，不会被截断
4. ✅ 问题描述中的"施工"、"监理"等词不会被误识别为单位名称

## 🧪 测试建议

### 功能测试
1. **重新导入玉岑铁路文档**：
   - 检查问题 59 和 60 的施工单位是否正确
   - 检查问题描述是否完整

2. **测试包含关键词的问题描述**：
   - 问题描述包含"施工"：如"施工现场存在安全隐患"
   - 问题描述包含"监理"：如"监理单位未到位"
   - 验证这些关键词不会被误识别为单位名称

3. **测试不同文档格式**：
   - 柳梧格式（三级结构）
   - 黄百格式（二级结构）
   - 验证两种格式都能正确识别

### 回归测试
- 测试之前正常识别的文档，确保修复没有引入新问题
- 对比修复前后的识别结果，确认只修复了错误识别的问题

## 📚 相关知识点

### 文档结构类型

**三级结构**（柳梧格式）：
```
（一）标段/工点行
  1. 工点名称
    （1）问题描述
    （2）问题描述
```

**二级结构**（黄百格式）：
```
1. 标段/工点行（包含施工单位、监理单位）
  （1）问题描述
  （2）问题描述
```

### 上下文变量

在解析过程中，以下变量保存当前的上下文信息：
- `current_contractor`：当前施工单位
- `current_supervisor`：当前监理单位
- `current_section_code`：当前标段编号
- `current_site_name`：当前工点名称
- `current_inspection_date`：当前检查日期

这些变量在遇到新的标段/工点行时更新，在提取问题描述时使用。

---

**修复日期**：2026-02-07  
**问题类型**：字段识别错误  
**影响范围**：其它安全质量问题章节  
**状态**：✅ 已修复

