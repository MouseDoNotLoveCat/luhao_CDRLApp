# 合湛铁路文档解析问题修复

## 问题描述

用户报告导入 `Samples/合湛铁路内部监督通知书（编号：南宁站[2026]（通知）合湛01号）-终版.docx` 时存在以下问题：

### 问题1：问题总数识别错误
- **系统识别**：47个问题
- **实际应该**：40个问题
- **差异**：多识别了7个问题

### 问题2：下发整改通知单问题1的施工单位识别失败
- **现象**：施工单位字段为空或显示"未知施工单位"
- **应该识别为**：中铁二十五局
- **位置**：第一个下发整改通知单的问题

### 问题3：下发整改通知单问题2重复识别
- **现象**：同一个问题被识别了3次
- **应该**：只识别为1个问题
- **位置**：第二个下发整改通知单的问题

### 问题4：HZZQ-1标的施工单位全部识别为"未知"
- **现象**：所有HZZQ-1标的问题，施工单位都显示为"未知施工单位"
- **应该识别为**：中铁二十五局
- **影响范围**：HZZQ-1标的所有问题

## 根本原因分析

### 原因1：施工单位提取逻辑不完整

**原有逻辑**只支持两种格式：
1. `由XXX施工` - 柳梧格式
2. `1.XXX施工` - 黄百数字行格式

**合湛文档可能使用的格式**：
- `中铁二十五局施工、XXX监理的...` - 直接以施工单位开头
- `施工单位：中铁二十五局` - 明确标注格式
- 其他变体格式

### 原因2：重复识别问题

可能的原因：
- 同一个问题的不同部分（检查情况、处理措施等）被识别为多个问题
- 段落分割逻辑有问题
- 问题边界判断不准确

### 原因3：施工单位回填失败

可能的原因：
- 标段编号（section_code）提取失败，导致无法建立映射
- 回填逻辑中的匹配条件不正确
- 上下文信息丢失

## 修复方案

### 修复1：增强施工单位提取逻辑

**修改文件**：`backend/app/parsers/word_parser.py`
**修改位置**：`_extract_contractor()` 方法（第 1046-1080 行）

**新增支持的格式**：

1. **格式3：直接以施工单位开头**
   ```
   中铁二十五局施工、XXX监理的...
   ```
   正则表达式：`^([^0-9（）\(\)一二三四五六七八九十]+?)施工[、，]`

2. **格式4：明确标注格式**
   ```
   施工单位：中铁二十五局
   ```
   正则表达式：`施工单位[：:]\s*(.+?)(?:[，、；;]|$)`

**修复后的代码**：
```python
def _extract_contractor(self, para: str) -> Optional[str]:
    """
    提取施工单位

    支持多种格式：
    - 格式1：由XXX施工、YYY监理的...
    - 格式2：1.XXX施工、/1．XXX施工，YYY监理的...
    - 格式3：XXX施工、YYY监理的...（直接以施工单位开头）
    - 格式4：施工单位：XXX
    """
    # 优先匹配"由XXX施工"形式
    match = re.search(r'由(.+?)施工', para)
    if match:
        return match.group(1).strip()

    # 兼容数字编号行开头：1.XXX施工 或 1．XXX施工
    match = re.search(r'^\d+[\.．]\s*([^、，]+?)施工', para)
    if match:
        return match.group(1).strip()
    
    # 匹配"施工单位：XXX"格式
    match = re.search(r'施工单位[：:]\s*(.+?)(?:[，、；;]|$)', para)
    if match:
        return match.group(1).strip()
    
    # 匹配直接以施工单位开头的格式：XXX施工、YYY监理
    # 但要排除以数字、括号等开头的情况
    match = re.search(r'^([^0-9（）\(\)一二三四五六七八九十]+?)施工[、，]', para)
    if match:
        contractor = match.group(1).strip()
        # 排除过短的匹配（可能是误匹配）
        if len(contractor) >= 3:
            return contractor

    return None
```

**关键改进**：
1. 添加 `.strip()` 去除首尾空格
2. 支持"施工单位："明确标注格式
3. 支持直接以施工单位开头的格式
4. 添加长度检查，避免误匹配过短的字符串

### 修复2：问题重复识别（需要进一步诊断）

**需要检查的地方**：
1. `_extract_rectification_notices()` 方法中的问题边界判断
2. 是否有多个条件同时触发了问题创建
3. 是否有循环逻辑导致重复添加

**建议的诊断步骤**：
1. 在问题创建前添加日志，记录触发条件
2. 检查 `issues.append(issue)` 被调用的所有位置
3. 验证问题描述的唯一性

### 修复3：施工单位回填失败（需要进一步诊断）

**需要检查的地方**：
1. `_postprocess_fill_units()` 方法中的回填逻辑
2. 标段编号（section_code）的提取是否正确
3. 映射字典（sec_map）的构建是否正确

**可能的修复方向**：
1. 增强标段编号提取逻辑
2. 添加更多的回填策略（如按标段名称回填）
3. 改进上下文信息的传递

## 测试建议

### 测试步骤

1. **重启后端服务器**：
   ```bash
   cd backend
   python3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

2. **清理 Python 缓存**：
   ```bash
   find backend -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null
   find backend -name "*.pyc" -delete 2>/dev/null
   ```

3. **上传合湛铁路文档**：
   - 在前端导入页面上传文档
   - 查看识别结果

4. **验证修复效果**：
   - 检查问题总数是否为40个
   - 检查下发整改通知单问题1的施工单位是否为"中铁二十五局"
   - 检查下发整改通知单问题2是否只识别了1次
   - 检查HZZQ-1标的所有问题的施工单位是否正确

### 预期结果

- ✅ 问题总数：40个（不是47个）
- ✅ 下发整改通知单问题1施工单位：中铁二十五局
- ✅ 下发整改通知单问题2：不重复
- ✅ HZZQ-1标施工单位：中铁二十五局

## 后续工作

如果修复1（增强施工单位提取）不能完全解决问题，需要：

1. **获取详细的诊断信息**：
   - 运行诊断脚本查看实际解析结果
   - 查看后端日志中的详细信息
   - 检查具体哪些问题被重复识别

2. **针对性修复**：
   - 根据诊断结果修复重复识别问题
   - 修复施工单位回填逻辑
   - 调整问题边界判断逻辑

3. **添加单元测试**：
   - 为合湛铁路文档格式添加测试用例
   - 确保修复不会影响其他文档的解析

## 相关文件

- **修改文件**：`backend/app/parsers/word_parser.py`
- **修改方法**：`_extract_contractor()` - 第 1046-1080 行
- **测试文档**：`Samples/合湛铁路内部监督通知书（编号：南宁站[2026]（通知）合湛01号）-终版.docx`

## 注意事项

1. **兼容性**：新增的提取逻辑不应影响现有文档（柳梧、黄百、玉岑）的解析
2. **性能**：正则表达式的顺序很重要，优先匹配最常见的格式
3. **边界情况**：注意处理空值、特殊字符等边界情况
4. **日志记录**：建议在关键位置添加日志，便于后续调试

