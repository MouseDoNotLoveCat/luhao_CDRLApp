# CDRLApp Git 管理指南

## 📋 Git 仓库初始化

✅ **已完成**
- 初始化 Git 仓库
- 配置用户信息
- 创建 .gitignore 文件
- 进行初始提交

## 🏷️ 版本标签

### 当前稳定版本

**标签**: `v1.0.0-stable`

**包含的功能**:
- ✅ ProjectSectionMatcher 类（项目和标段匹配）
- ✅ 完整的导入功能（FormData 处理）
- ✅ NoticesList.vue 代理组件（Vite 缓存修复）
- ✅ 动态 Content-Type 处理
- ✅ 所有导入错误已解决

**修复的问题**:
- ✅ HTTP 422 Pydantic 验证错误
- ✅ HTTP 500 后端 ImportError
- ✅ Vite 编译错误
- ✅ FormData Content-Type 处理

## 🌿 分支管理

### 主分支

**master** - 主开发分支
- 包含所有稳定的代码
- 每个重要版本都有对应的标签

### 功能分支

**fix/import-functionality-pydantic-validation** - 导入功能修复分支
- 用于修复 HTTP 422 错误
- 已合并到 master

## 📝 常用 Git 命令

### 查看提交历史
```bash
git log --oneline
git log --oneline --all --graph
git log --oneline -10
```

### 查看标签
```bash
git tag -l
git tag -l -n5
git show v1.0.0-stable
```

### 创建新分支
```bash
git checkout -b feature/new-feature
git checkout -b fix/bug-fix
```

### 切换分支
```bash
git checkout master
git checkout feature/new-feature
```

### 提交代码
```bash
git add .
git commit -m "描述性的提交信息"
```

### 合并分支
```bash
git checkout master
git merge feature/new-feature
```

### 回滚代码
```bash
# 回滚到特定提交
git reset --hard <commit-hash>

# 回滚到特定标签
git reset --hard v1.0.0-stable

# 回滚到上一个提交
git reset --hard HEAD~1
```

## 🔄 回滚操作

### 场景 1: 回滚到稳定版本

```bash
# 查看稳定版本
git tag -l

# 回滚到稳定版本
git reset --hard v1.0.0-stable

# 验证
git log --oneline -5
```

### 场景 2: 回滚到特定提交

```bash
# 查看提交历史
git log --oneline

# 回滚到特定提交
git reset --hard cf4482e

# 验证
git log --oneline -5
```

### 场景 3: 撤销最后一次提交

```bash
git reset --hard HEAD~1
```

## 📊 Git 状态检查

```bash
# 查看当前状态
git status

# 查看当前分支
git branch

# 查看所有分支
git branch -a

# 查看当前提交
git log -1
```

## 🎯 最佳实践

1. **定期提交** - 每完成一个功能或修复就提交
2. **清晰的提交信息** - 使用描述性的提交信息
3. **使用分支** - 为每个功能或修复创建新分支
4. **创建标签** - 为重要版本创建标签
5. **定期备份** - 定期备份 .git 目录

## 📌 重要文件

- `.git/` - Git 仓库数据
- `.gitignore` - 忽略文件列表
- `GIT_MANAGEMENT_GUIDE.md` - 本指南

## ✅ 总结

✅ Git 仓库已初始化  
✅ 初始提交已完成  
✅ 稳定版本标签已创建  
✅ 可以随时回滚代码  

现在您可以安心进行开发，需要时可以随时回滚到稳定版本！

